# Release Workflow

Release only when:
- acceptance criteria pass;
- relevant tests pass;
- critical bugs are resolved;
- security/privacy review passes;
- environment configuration is documented;
- rollback is known;
- monitoring is available.

If a release blocker exists, stop.
